/**
 * Discord event handlers.
 *
 * Registers handlers for:
 * - messageCreate: routes incoming messages to the appropriate agent
 * - ready: logs bot startup
 * - error: handles client errors
 */

// TODO: Register event handlers on the Discord client
// - On messageCreate: ignore bots, pass to router
// - On ready: log startup info
// - On error: log and optionally alert
